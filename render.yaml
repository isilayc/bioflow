services:
  - type: web
    name: omicsroute-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn api.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: OMICSROUTE_CORS_ORIGINS
        sync: false
